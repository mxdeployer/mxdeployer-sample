on: [push]

jobs:
  mxdeployer-stage-sample:
    runs-on: ubuntu-latest
    environment: stage
    steps:
    - name: Display the secret message
        uses: mxdeployer/mxdeployer-action@v0.3
        with:
          message: "${{ secrets.MESSAGE }}"
